[pytest]
testpaths = tests
pythonpath = .
addopts = --tb=short -ra --strict-markers
filterwarnings =
    ignore::UserWarning:presidio_analyzer.*
    ignore::DeprecationWarning:google.genai.*
markers =
    slow: touches every attachment in the dataset
