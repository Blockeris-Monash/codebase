# --- NEVER COMMIT: organiser answer key ---------------------------------
# The organiser Docker kit (sdoc-hackathon-docker) ships ground_truth.json.
# It lives outside this repo on purpose. These rules are a second line of
# defence in case anyone copies it in.
ground_truth.json
**/ground_truth.json
sdoc-hackathon-docker/
sdoc-hackathon-docker*.zip
data_v2/
secrets/

# The provided dataset IS committed - organisers confirmed 19 Sep that it
# may go in a public repo, so judges can clone and run without a download.
# The answer key above is a separate thing and stays out.

# --- Secrets -------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key

# --- Python --------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# --- Node ----------------------------------------------------------------
node_modules/
dist/
build/
.next/

# --- Local ---------------------------------------------------------------
.DS_Store
*.log
submission.json

.venv/
