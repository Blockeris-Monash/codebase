{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ClassificationResult",
  "title": "ClassificationResult",
  "description": "STAGE 2 of 5  |  produced by: classify (R2)  |  consumed by: R1, R3\n\nOne per email, all 520. Only BL_COMPARISON continues to stage 3. decided_by is read by the official scorer, which reports the share of decisions made by rule.",
  "type": "object",
  "required": [
    "email_id",
    "category",
    "decided_by",
    "confidence",
    "evidence"
  ],
  "additionalProperties": false,
  "properties": {
    "email_id": {
      "type": "string",
      "pattern": "^(email_[0-9]{3}|gmail_[0-9a-f]{8,32})$"
    },
    "category": {
      "enum": [
        "BL_COMPARISON",
        "SI_REQUEST",
        "INVOICE_QUERY",
        "GENERAL",
        "SPAM"
      ]
    },
    "decided_by": {
      "enum": [
        "rule",
        "llm"
      ]
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    },
    "evidence": {
      "type": "string",
      "description": "Why this category. Shown to a human in the review UI."
    }
  }
}
