{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ClassificationResult",
  "title": "ClassificationResult",
  "description": "Contract 2. One per email, all 520. Produced by R2, consumed by R1 and R3. decided_by is read by the official scorer, which reports the share of decisions made by rule.",
  "type": "object",
  "required": ["email_id", "category", "decided_by", "confidence", "evidence"],
  "additionalProperties": false,
  "properties": {
    "email_id":   { "type": "string", "pattern": "^email_[0-9]{3}$" },
    "category":   { "enum": ["BL_COMPARISON", "SI_REQUEST", "INVOICE_QUERY", "GENERAL", "SPAM"] },
    "decided_by": { "enum": ["rule", "llm"] },
    "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
    "evidence":   { "type": "string", "description": "Why this category. Shown to a human in the review UI." }
  }
}
