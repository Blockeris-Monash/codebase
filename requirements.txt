# Pinned exactly, not floated.
#
# These were `>=` until a release between a green CI run and the judging window
# could have changed what Render built without anything going red - CI installs
# the same floating versions at the same moment, so it would have agreed with
# the broken deploy. The README promises this works from a clean clone; that
# promise is only as good as the versions it installs.
#
# Bump deliberately: change a line, run the suite, commit the result.

# Runtime
fastapi==0.141.1
uvicorn==0.53.0
pydantic==2.13.5
python-dotenv==1.2.3
pypdf==6.19.0
google-genai==2.24.0
presidio-analyzer==2.2.364
presidio-anonymizer==2.2.364
httpx==0.28.1            # also the Gmail calls for the live mailbox (backend/gmail.py)


# Reply drafting over the company guidelines (backend/reply.py,
# tools/ingest_policies.py). Pinned at what `>=` resolved to on the day they
# were added: langchain-text-splitters>=0.2.0 already installs 1.1.2, a major
# version past the floor, so the floor was never describing what CI ran.
supabase==2.31.0
langchain-text-splitters==1.1.2

# Development
pytest==9.1.1
